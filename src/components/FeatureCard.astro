---
const { icon, title, text } = Astro.props;
---

<div class="max-w-sm mx-auto text-center">
  <img src={icon} alt={title} class="mx-auto mb-6" />
  <h3 class="font-bold text-lg">{title}</h3>
  <p class="text-gray-300 text-sm mt-3">{text}</p>
</div>
